<p>Useful CSS functions include <code>var()</code>, <code>calc()</code>, <code>min()</code>/<code>max()</code>/<code>clamp()</code>.</p>

<h2>Examples</h2>
<pre><code class="language-css">.sidebar { width: clamp(16rem, 20vw, 24rem); }
.card { padding: calc(var(--space) * 1.5); }
</code></pre>
